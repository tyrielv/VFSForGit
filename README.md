# PR Assets

Screenshots and other binary assets referenced from pull request descriptions.

Each subdirectory under `.pr-assets/` corresponds to a PR (named for the head branch).
This branch has no parent commit, so the binaries do not bloat the main repository history.